<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Snippets</title>
</head>
<body>
    <!-- Main content area -->
    <div class="container-fluid">
        <div class="row">
            <!-- Vertical navbar -->
            <%- include ('partials/navbar.ejs') %>
            
            <!-- Content area -->
            <div class="col mt-3">
                <form action="/notes/<%= note.noteID %>/" method="POST">
                    <div class="row">
                        <div class="col">
                            <input type="text" placeholder="Title" id="titleNew" class="input input-title" name="title" value="<%= note.title %>">
                        </div>
                        <div class="col d-flex justify-content-end">
                            <a href="/notes/<%= note.noteID %>/delete" class="btn btn-danger me-2 notes-btn">Delete</a>
                            <button type="submit" class="btn btn-primary me-2 notes-btn">Save</button>
                        </div>
                    </div>
                    <div class="row d-flex">
                        <div class="col">
                            <input type="text" placeholder="Category" id="categoryNew" class="input mt-3 input-category" name="category" value="<%= note.category %>">
                        </div>
                        <div class="col"></div> <!-- Empty col so the previous col takes only 50% space -->
                    </div>
                    <div class="row">
                        <textarea placeholder="Content" id="contentNew" class="input mt-3 input-textarea" rows="20" name="content"><%= note.content %></textarea>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <!-- CSS Styles -->
    <style>
        .input-title {
            font-size: xx-large;
            width: 100%;
            border: 0px;
            border-bottom: 2px solid black;
            transition: all 0.2 ease;
        }
        .input-title:focus {
            outline: none !important;
            border-bottom: 2px solid #8a00ff;
        }
        .input-category {
            width: 100%;
            border: 0px;
            border-bottom: 2px solid black;
            transition: all 0.2 ease;
        }
        .input-category:focus {
            outline: none !important;
            border-bottom: 2px solid #8a00ff;
        }
        .input-textarea {
            width: 100%;
            border: 0px;
            transition: all 0.2 ease;
            background-image: radial-gradient(rgb(214, 214, 214) 2px, transparent 0);
            background-size: 24px 24px;
            background-position: 0px 15px;
        }
        .input-textarea:focus {
            outline: none !important;
        }
        .notes-btn {
            display: flex;
            width: 100px;
            align-items: center;
            justify-content: center;
            letter-spacing: 1px;
            cursor: pointer;
            border: none;
            font-size: large;
            background: linear-gradient(to bottom, #a352aa, #8a00ff);
            transition: all 0.5s ease;
        }
    </style>
</body>
</html>
