<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Snippets</title>
</head>
<body>
    <!-- Main content area -->
    <div class="container-fluid">
        <div class="row">
            <!-- Vertical navbar -->
            <%- include ('partials/navbar.ejs') %>
            
            <!-- Content area -->
            <div class="col mt-3">
                <form action="/notes/<%= note.noteID %>/" method="POST">
                    <div class="d-none">
                        <input type="text" placeholder="Id" id="id" class="input" name="id" value="<%= note.id %>">
                    </div>
                    <div class="row">
                        <div class="col">
                            <input type="text" placeholder="Title" id="titleNew" class="input" name="title" value="<%= note.title %>">
                        </div>
                        <div class="col d-flex justify-content-end">
                            <button type="submit" class="btn btn-primary me-2 notes-btn">Save</button>
                        </div>
                    </div>
                    <div class="row">
                        <input type="text" placeholder="Category" id="categoryNew" class="input" name="category" value="<%= note.category %>">
                    </div>
                    <div class="row">
                        <textarea placeholder="Content" id="contentNew" class="input" name="content"><%= note.content %></textarea>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <!-- CSS Styles -->
    <style>
        .notes-btn {
            display: flex;
            width: 100px;
            align-items: center;
            justify-content: center;
            letter-spacing: 1px;
            cursor: pointer;
            border: none;
            background: linear-gradient(to bottom, #a352aa, #8a00ff);
            transition: all 0.5s ease;
        }
    </style>
</body>
</html>
